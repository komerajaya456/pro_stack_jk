import React  from 'react';
import ReactDOM from 'react-dom/client';

import Jk from './components/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Jk />
  </React.StrictMode>
);
